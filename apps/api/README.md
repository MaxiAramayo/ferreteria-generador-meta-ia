# API

Backend modular NestJS. La API expone operaciones autenticadas, OAuth y
webhooks, pero no ejecuta generación ni publicación de larga duración dentro de
la solicitud HTTP.

El bootstrap pertenece a `P0-T05`.
