# Database

PostgreSQL será la fuente de verdad de publicaciones, programación, conexiones,
auditoría y snapshots comerciales.

El ORM, la versión del motor y la estrategia de migraciones se fijan en
`P0-T02` y `P2-T01`. No se permite sincronización automática destructiva en
producción.
